{
  "name": "magnoliametrics.com",
  "source": "index.html",
  "scripts": {
    "start": "live-server --port=8000",
    "build": "mkdir -p dist/assets/css dist/assets/js && cp -r assets/css/* dist/assets/css/ && cp -r assets/js/* dist/assets/js/ && cp CNAME dist/"
  },
  "dependencies": {
    "bulma": "^0.9.3"
  }
}
